# Install these into the existing Airflow environment using that environment's
# official Airflow constraints file. Apache Airflow itself is supplied by the deployment.
pandas>=2.1,<3
numpy>=1.26,<3
requests>=2.31,<3
beautifulsoup4>=4.12,<5
yfinance>=0.2.40,<2
plotly>=6.1.1,<7
kaleido>=1.0,<2
Jinja2>=3.1,<4
python-dotenv>=1.0,<2
